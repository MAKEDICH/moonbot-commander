# ============================================================
# MOONBOT COMMANDER - DOCKER ENVIRONMENT VARIABLES
# ============================================================
# Copy this file to .env and configure as needed
# ============================================================

# Application mode: server or local
MOONBOT_MODE=server

# Database URL (SQLite by default, stored in Docker volume)
# Note: Use absolute path for Docker
DATABASE_URL=sqlite:////app/data/moonbot_commander.db

# CORS origins (comma-separated)
# Add your domain if using reverse proxy
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# API URL for frontend (change if using reverse proxy)
VITE_API_URL=http://localhost:8000

# Ports (change if needed)
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ============================================================
# OPTIONAL: Security settings
# ============================================================

# JWT secret key (auto-generated if not set)
# JWT_SECRET_KEY=your-super-secret-key-here

# 2FA encryption key (auto-generated if not set)
# ENCRYPTION_KEY=your-encryption-key-here

# ============================================================
# OPTIONAL: UDP settings for MoonBot communication
# ============================================================

# UDP port for bot commands (default: 2500 in server mode)
# UDP_PORT=2500

# UDP timeout in seconds
# UDP_TIMEOUT=5

